name: Active decision identification

on:
  pull_request:
    paths:
      - ".github/workflows/active-decision-identification.yml"
      - "src/causal4d/active_decision_identification.py"
      - "tests/test_active_decision_identification.py"
      - "scripts/experiments/active_decision_identification.py"
      - "docs/active_decision_identification.md"
  push:
    branches: [main]
    paths:
      - ".github/workflows/active-decision-identification.yml"
      - "src/causal4d/active_decision_identification.py"
      - "tests/test_active_decision_identification.py"
      - "scripts/experiments/active_decision_identification.py"
      - "docs/active_decision_identification.md"
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: active-decision-identification-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  validate:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - name: Check out exact revision
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false
          clean: true

      - name: Set up Python 3.12
        uses: actions/setup-python@5fda3b95a4ea91299a34e894583c3862153e4b97 # v7.0.0
        with:
          python-version: "3.12"

      - name: Install focused dependencies
        run: |
          python -m pip install --disable-pip-version-check \
            numpy==2.2.6 \
            pytest==8.4.2

      - name: Run focused tests
        env:
          PYTHONPATH: src
        run: pytest -q tests/test_active_decision_identification.py

      - name: Execute deterministic mechanism study
        env:
          PYTHONPATH: src
        run: |
          python scripts/experiments/active_decision_identification.py \
            --output build/active-decision-identification/result.json
          python -m json.tool \
            build/active-decision-identification/result.json >/dev/null

      - name: Upload result
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v6.0.0
        with:
          name: active-decision-identification-${{ github.sha }}
          path: build/active-decision-identification/result.json
          if-no-files-found: error
          retention-days: 30
